# ── Docker DNS resolver ────────────────────────────────────
# Include inside any `location` or `server` block that needs
# to resolve Docker service names dynamically.
resolver 127.0.0.11 valid=30s;
